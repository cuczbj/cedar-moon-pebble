# 本次实现与运行结果

已按原始Step1至Step16及补充说明运行附件1全部100条样本，未训练情感预测模型。

## 实际输出

| 字段 | full形状 |
| --- | --- |
| text | 100 × 88 × 768 |
| audio | 100 × 88 × 25 |
| vision | 100 × 88 × 50 |
| R_occ、R_raw、R_rollout | 100 × 88 × 65 |
| R_word | 100 × 65 × 65 |

实际有效WordPiece长度7至84，6条超过50；full补齐至88，compat50只裁剪行，保留全部单词列。

三个特征文件共13,229,096字节（12.616 MiB），未删除R_raw、R_rollout或帧级数据。100份逐词元alignment_records CSV、全量汇总表、环境及质量报告、三张300-dpi图均已生成。

## 已完成的自动验证

- 100条清单、PTS解码、音频重采样、官方文本映射、强制对齐、BERT编码、语音特征、视觉特征均完成。
- 100条单句与batch编码误差均为0，低于1e-5门槛。
- 100条R_occ行和、非负性、重复计算检查通过。
- 100条float16语音和视觉池化重算及文本重算通过。
- 重新读取正式PKL和q1_frames.npz后，full和compat50各100条池化重算通过。
- 16项单元及集成测试通过；真实导出样本的trace_sample读取与权重分配验证通过。
- 三张图已打开检查可读性。

这些验证说明实现和存储对应一致，不代表已经人工确认每个词的真实发音边界，也不代表遮挡敏感度是情感因果贡献。

## 需要人工复核的内容

1926个有语音单词均返回时间区间，其中280个低于0.5置信度阈值，列表见outputs/meta/low_confidence_alignment.csv。

以下5条样本在词元池化后没有有效视觉位置，已经保留样本、零特征和明确无效掩码：

- -NFrJFQijFE$_$1
- -NFrJFQijFE$_$2
- -mJ2ud6oKI8$_$1
- -mJ2ud6oKI8$_$2
- -ri04Z7vwnc$_$0

不得把这些样本当作完整可靠的人脸观测。应回看原视频判断是否无人脸、遮挡、尺度或检测置信度问题，再决定是否有依据调整工具配置。

## 实现边界与运行选择

- 必需的单词级遮挡方法及辅助Attention Rollout已实现。可选梯度方法R_grad未实现，不能将其标成已验收。
- 默认WhisperX对齐实际运行通过；MFA适配代码已提供，但本机未安装MFA，未进行真实MFA端到端验证。
- OpenFace 2.2使用发布包自带CLNF关键点模型，静态AU；分析帧使用JPEG质量95，逐样本临时解码后释放缓存。原视频未修改。
- BERT使用固定padding与逐词元等价线性算子以稳定float32批次舍入；模型权重未训练或修改。配置及模型哈希见environment.json。
- 全部特征和矩阵计算使用float32，导出使用float16；帧级时间保留float32，词时间及CSV保留更高精度。
- OpenSMILE内置短时平滑、OpenFace跟踪及动态模式可能扩大依赖范围；文档中没有将它们虚构成严格单位感受野。
- 预测模型、情感任务性能、时间重要性学习均不在本阶段范围内。
